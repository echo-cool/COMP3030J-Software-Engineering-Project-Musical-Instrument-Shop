pipeline {
    agent {
        docker {
            image 'python:3.9-buster' 
            args '-v /root/.m2:/root/.m2' 
        }
    }
    stages {
        stage('Pre-Tesks') { 
            steps {
                sh 'ls' 
            }
        }
        stage('Build') { 
            steps {
                sh 'ls' 
            }
        }
        stage('Test') { 
            steps {
                sh 'ls' 
            }
        }
        stage('Post-Tasks') { 
            steps {
                sh 'ls' 
            }
        }
    }
}